{% if (item) %}
    <img src="{{ generate_preview_link(item.fileName, class, width, height ) }}" alt="{{ item.id }}">
{% else %}
    <img src="/assets/dashboard/img/default-150x150.png" alt="#">
{% endif %}
