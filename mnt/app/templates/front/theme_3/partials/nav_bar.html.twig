<nav class="some-header-nav" role="custom-dropdown">
    <ul>
        {% for category in categories %}
            <li>
                <a href="{{ path('front.show_by_category', {
                    'slug': category.slug
                }) }}" target="_blank">
                    {{ category.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
</nav>

<span class="icon" onclick="openMenu()">☰</span>

<div class="header-my-links" id="header-my-links">
    {% for category in categories %}
        <a href="{{ path('front.show_by_category', {
            'slug': category.slug
        }) }}" target="_blank" class="some-links">
            {{ category.title }}
        </a>
    {% endfor %}
</div>